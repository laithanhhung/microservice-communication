---

---

<div class="relative w-full h-full flex items-center justify-center">
    <div
        class="w-48 h-48 rounded-full bg-white shadow-xl border-4 border-gray-100 flex flex-col overflow-hidden relative z-10"
    >
        <!-- Upper Hub -->
        <div
            class="h-1/2 bg-blue-400 flex items-center justify-center text-white font-bold text-center p-2 text-sm leading-tight"
        >
            Asynchronous<br />Non-blocking
        </div>
        <!-- Lower Hub -->
        <div
            class="h-1/2 bg-purple-500 flex items-center justify-center text-white font-bold text-center p-2 text-sm leading-tight"
        >
            Synchronous<br />Blocking
        </div>

        <!-- Connection Points (Invisible anchors for positioning context if needed, but styling is mostly visual here) -->
    </div>

    <!-- We can overlay the 'TopicNode' here if using the original visual style where it overlaps,
         but for Grid layout, this component just represents the Switch Circle -->
</div>
